# Bibliotecas principais do projeto
mediapipe>=0.10.0
torch>=1.13.0
torchvision>=0.14.0
facenet-pytorch>=2.5.3
opencv-python>=4.8.0
numpy>=1.21.0

# Para manipulação de imagens (usado internamente pelo facenet-pytorch e OpenCV)
Pillow>=10.4.0

# Não necessário instalar separadamente (faz parte da lib padrão), mas listado para documentação
# sqlite3 (builtin do Python)

# Opcional: se quiser usar playsound para alertas sonoros (como no seu código original)
# playsound3>=0.1.2